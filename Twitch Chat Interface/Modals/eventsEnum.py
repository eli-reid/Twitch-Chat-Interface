from enum import Enum
class eventsEnum(object):
    """Event Types"""
    MESSAGE = "MESSAGE"
    JOINED = "JOINED"
    RECEIVED = "RECEIVED"
    CONNECTED = "CONNECTED"
    ERROR = "ERROR"
    DISCONNECTED = "DISCONNECTED"

